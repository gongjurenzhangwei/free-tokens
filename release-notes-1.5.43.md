# v1.5.43

加密备份导入/导出的关键修复与若干健壮性改进。

## 🐛 关键修复（Critical / High）

- **C1 修复：v2 加密备份导出的 API Key 无法导入。** 之前带用户密码导出时，密钥会先被「工作区自动 passphrase」二次加密，导致导入时用用户密码永远解不开、密钥全部被跳过。现在 v2 导出把密钥明文放进已被 AES-256-GCM 整体加密的信封内，导入时用用户密码即可正常恢复。
- **H1 修复：密码输入错误后无法重试。** 之前密码错误会清空挂起的导入状态并弹双重错误提示。现在保留挂起状态，改回正确密码即可直接重试，且不再重复弹错。
- **H2 修复：旧版（v1 legacy）备份不再被强弹密码框。** legacy 密钥是用导出方工作区的自动密码加密的，没有用户密码可输入。现在直接导入，并依次尝试当前工作区的自动密码；跨工作区时密钥安全跳过、Plan 等其余数据正常导入。

## ✨ 健壮性与安全改进

- **M2** 前端 iframe 消息来源校验：远程免费 Token 页面无法再伪造高度/跳转消息；后端 `saveSettings` 改为白名单字段校验，防止伪造配置覆盖本地设置。
- **M3** dashboardView 中 meta 标签（版本、微信二维码、免费 Token URL）插入前做 HTML 属性转义，防止配置注入。
- **M4** Responses 协议统一过滤 `_` 前缀的内部选项，与 OpenAI/Anthropic 协议行为一致。
- **M6** 前端 `Copy` 类型收紧：8 个子组件的 `text: any` 改为类型化 `text: Copy`，获得 key 拼写与参数检查。
- **M1** 清理后端已废弃的 i18n key（密码弹窗文案已由前端接管）。
- **M5** 新增 `npm test`：加密导入/导出往返验证脚本（13 个断言）。
- **L9/L10** 清理 favicon 域名重复规则与死代码 `_legacyFreeTokensView`。

## 🧪 测试

- `npm run check` / `npm run compile` 通过。
- `npm test`：13/13 通过（v2 往返恢复、错误密码重试、legacy 同/跨工作区、无密钥导出）。
