# v1.5.45 — tooltip 配额进度条同步像素风格

## 优化内容

- **悬停 tooltip 的官方配额进度条同步改为像素块风格**：将 `quotaRows` 的连续圆角条（8px 高、宽度填充）替换为 **10 段独立色块**（8×10px 圆角小块、3px 间距），已用段用阈值彩色填充、未用段留空槽（边框色），与状态栏本体的 `█`/`░` 像素进度条风格呼应。
- **彩色保留**：已用像素块仍按占用比例着色——≥90% 红色、≥70% 琥珀色、其余青色，与状态栏文字颜色及配额风险语义一致。
- 不限量（UNLIMITED）场景显示 10 段空槽。

## 影响文件

- `src/statusPanel.ts`：`quotaRows()` 的进度条从连续条改为 10 段像素块。
