# v1.5.47 — 状态栏配额显示剩余百分比 + 下次重置时间

## 需求
状态栏官方配额改为更直观的展示方式。

## 改动
- **src/extension.ts `updateStatus()`**（配额分支）：
  - 去掉窗口标签（如「5 小时（滚动）」），默认取第一个窗口（即 5 小时滚动窗口）
  - 百分比改为**剩余百分比**（剩余 = 100 - 已用），显示为 `剩 62%`（英文 `62% left`）
  - 新增**下一次重置时间**：当天显示 `重置 14:30`，跨天显示 `重置 08/10 14:30`（英文 `reset HH:MM`）
  - 保留像素进度条（`█` 已用 / `░` 未用）+ 阈值彩色（≥90% 红 / ≥70% 琥珀 / 其余青），unlimited 时不显示像素条
  - 新增 `formatResetTime()` 辅助函数：重置时间短格式（当天 HH:MM，跨天 MM/DD HH:MM）
- 未知 resetAt 或不限量时不显示重置时间；无百分比但只有剩余量时回退显示 `剩余 X`

## 显示效果示例
- 有配额数据：`$(key) BYOK ██████░░░░ 剩 38% 重置 14:30`
- 不限量：`$(key) BYOK 不限量`

## 验证
- `npm run check`（tsc --noEmit）通过
- `npm test`（加密往返 13/13）通过
