# v1.5.50 — 状态栏样式固定（重新发布）

## 需求
用户反馈升级后状态栏仍旧看不见，怀疑本地扩展未刷新到新版本。本次重新发布，确保包含 v1.5.49 的状态栏样式修复。

> 用户原话：「当额度快完的时候，右下角状态栏的颜色会变成白色，导致文字看不见了。文字的颜色不能变，必须始终保持可见。状态栏的样式不要随用量的变化而变化，要始终保持可见的状态」。

## 改动
- 状态栏不再随用量动态变色（v1.5.44 的 `quotaColor()` 已删除），文字颜色固定，由主题保证始终可读
- 保留像素进度条 `pixelBar()`（█/░ 字符），作为颜色无关的用量指示

## 验证
- `npm run check` 通过
- `npm run compile` 通过
- `npm test` 通过（13/13）
- `npm run package:vsix` 通过