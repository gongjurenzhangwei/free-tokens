# v1.5.54 修复：备份导入后 Token 总量翻倍

## 问题
重新安装插件并从备份导入后，概览 Token 总量从约 6 亿跳到 10.9 亿（约 2 倍）。

## 根因（两个叠加缺陷）
1. **重复计数（主因）**：备份同时包含 `usage`（原始记录）与 `usageDaily`（按日聚合）。导入时先把备份的原始记录写入 `USAGE_KEY`，随后日聚合合并调用内部迁移重建逻辑时，会基于**已含备份原始记录的 `USAGE_KEY`** 重建日聚合，再与备份自带的 `usageDaily` 累加——同一天的 Token 被算了两次。
2. **策略失效（次因）**：无论导入时选择「替换全部现有 Plan」或「跳过同名 Plan」，用量数据都无条件累加合并，旧数据 + 新数据会叠加。

## 修复
- `importConfig` 日/小时聚合合并改为**显式基于导入前的本地快照**合并基准（空聚合时用导入前的原始记录重建），彻底避免与备份原始记录重建结果重复累计。
- 用量数据（原始记录 / 日聚合 / 小时聚合）严格按导入策略处理：
  - **替换全部现有 Plan**（replace）：用量直接覆盖，不叠加
  - **跳过同名 Plan**（skip）：跳过用量写入
  - **按 ID 合并**（merge）：才逐 key 累加
- 新增回环测试场景验证 4 种组合（空库导入 / merge 重复累加 / replace 覆盖 / skip 跳过），17/17 通过。
- 顺带修复：根目录备份文件（`free-tokens-with-keys-*.json`）曾被打进扩展安装包，已移出并补充 `.vscodeignore` 排除规则。

## 验证
`npx tsc --noEmit` + `npm run compile` + `npm test`（17/17）+ `npm run package:vsix`（free-tokens-1.5.54.vsix，381.42 KB，25 文件）全通过。