# v1.5.55 新增：清空全部数据按钮

## 新功能
- 设置页新增「清空数据」区块，可一键恢复扩展到初始状态。

## 清空范围（全部重置）
- **Plan**：所有计划配置
- **API Key**：SecretStorage 中按计划的密钥
- **用量统计**：原始记录、按日聚合（`usageDaily`）、按小时聚合（`usageHourly`，趋势图数据源）
- **配额快照**
- **设置**：主题、语言、状态栏用量显示等偏好

## 安全设计
- 点击清空按钮后弹出 **VS Code 原生确认对话框**（模态），明示"此操作不可撤销、建议先导出一份备份"，必须显式点击「清空」才会执行。
- 特意**保留**工作区级随机导出口令（`exportPassphrase`）：它只是用来加密 v1 旧版备份的随机值，删除它既无法"更干净"，还会导致旧备份无法解密。
- 清空完成后自动刷新 Copilot 提供者与前端状态，并提示"已恢复到初始状态"。

## 验证
- 新增回环测试场景 7 覆盖清空路径：造 2 个 Plan + 2 个密钥 + 用量 + 配额 + 设置后清空，断言计划/用量/日聚合/小时聚合/配额/设置全部清空、密钥删除、导出口令保留，17 项新增断言 + 原有 17 项 = 34/34 全部通过。
- `npx tsc --noEmit` + `npm run compile` + `npm test`（34/34）+ `npm run package:vsix` 全通过。