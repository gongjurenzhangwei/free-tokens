# v1.5.56 优化：配额来源支持全部官方配额渠道

## 改进
- 设置里的「配额来源」下拉此前只列出 MiniMax，现在改为列出**所有支持官方配额查询接口的渠道**（OpenCode Go、MiniMax 等），有官方配额数据的渠道都可选作状态栏配额来源。
- 模型行（Plan 列表）的「刷新配额」按钮也同步改为"支持官方配额接口就显示"，不再限 MiniMax，OpenCode Go 等渠道同样可手动刷新。

## 说明
- 「配额来源」只在状态栏显示模式选择「官方配额」时出现，用于指定状态栏展示哪一个渠道的实时官方配额（剩余百分比 + 下次重置时间）。

## 验证
- `npx tsc --noEmit` + `npm run compile` + `npm test`（34/34）+ `npm run package:vsix` 全通过。