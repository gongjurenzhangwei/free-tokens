# v1.5.57 优化：下拉、清空按钮与免费 Token 卡片 UI 修复

## 改进
- **下拉样式美化**：「配置来源」「状态栏显示」等原生下拉菜单（含模型筛选、思考深度、表单下拉）统一加上了自定义箭头、悬停描边与聚焦高亮，不再是最简陋的浏览器原生样式。
- **清空按钮对齐修复**：「清空数据」「清空全部数据」按钮的图标与文字此前水平未对齐，已加入与其它主按钮一致的 flex 布局（inline-flex + 居中 + 间距），且保留原有的危险态红边样式。
- **免费 Token 卡片深色模式修复**：「免费 Token」页此前在深色模式下仍显示为白色卡片，现改为跟随仪表盘主题：深色模式正确显示深色卡片，亮/暗切换实时同步。

## 说明
- 「免费 Token」卡片通过 iframe 内嵌渲染，属于独立文档，无法继承主界面主题，本次通过 `postMessage` 通知内嵌页面当前主题完成同步。

## 验证
- `npx tsc --noEmit` + `npm run compile` + `npm test`（34/34）+ `npm run package:vsix` 全通过。