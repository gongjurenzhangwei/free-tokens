# v1.5.59 UI 调整：趋势图选中标签文字改为白色

## 改进
- **趋势图选中标签对比度优化**：「模型调用趋势」图上方分段控件（调用量 / Token 用量，以及 24 小时 / 7 天 / 30 天等时间范围）在被选中时背景为青色、文字为深色，对比不够清晰；现将选中态文字改为白色并加粗（`#fff` + `font-weight: 700`），与青色背景形成清晰对比，选中状态一目了然。

## 说明
- 纯前端样式改动，仅调整 `styles.css` 中 `.trend-toggle button.is-active` 的选中态颜色，不影响任何数据逻辑、接口字段或行为。
- 非选中态、悬停态样式保持不变。

## 验证
- `npx tsc --noEmit` + `npm run compile` + `npm test`（34/34）+ `npm run package:vsix` 全通过。