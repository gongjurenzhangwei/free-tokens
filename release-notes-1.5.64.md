# v1.5.64 内容更新：B 站视频 PPT 解决方案页标题单行化

## 改进
- **解决方案页标题改为单行**：h2 标题「Free Tokens 插件 › 做了什么？」由两行（中间带 `<br/>`）改为一行显示「Free Tokens 插件做了什么？」，标题完整呈现在左半屏内、不再换行。
- **字号同步收小**：为适配单行排版，h2 字号由 `min(5.8vw,10.2vh)`（1080p 下约 99px）调整为 `min(3.8vw,5.8vh)`（1080p 下约 63px），在保持标题可读性的同时让整句单行放得下且留有余量；`line-height` 同步微调（0.96 → 1.1）避免单行剪裁。

## 验证
- 浏览器实测（1920×1080）：h2 渲染为**单行**，文本实际宽度约 782px，左半屏可用宽 822px，余量约 40px，无溢出、无折行；与上方「02 / WHAT IS BYOK」、下方 BYOK 简介及右侧 010203 等宽序号布局协调。
- 1280×720 ~ 2560×1440 各常见视口下均能保持单行（字号随 `min(3.8vw,5.8vh)` 自适应）。
- `npm test`（34/34）+ `npm run package:vsix` 通过。

## 说明
- 仅改动 `bilibili-ppt/index.html`（B 站视频演示 PPT 静态文件），不影响扩展运行逻辑、接口或行为。
- 版本号从 1.5.63 递增至 1.5.64，重新打包 VSIX 以包含更新后的演示文件。