# v1.5.67 功能更新：Dashboard「用量与配额」显示下次重置时间

- **「用量与配额」视图**：每个带 used/limit 数据的配额行新增「下次重置」时间，与状态栏重置时间逻辑一致。
- 重置时间格式：当天仅显示 `HH:MM`（如 `15:46`），跨天显示 `MM/DD HH:MM`（如 `09/21 15:46`）。
- 中英文文案：中文「下次重置」/ 英文「next reset」。
- 数据来源于各供应商套餐已解析的 `resetAt` 字段（LocalDateTime / expires_at / end_time / resetsAt 等），未解析到重置时间的套餐不显示该提示。
- 已适配 1080p 及更高分辨率，无溢出。