# v1.5.68 修复：检查更新错误提示

- 修复「检查更新失败：GitHub responded 404」——该错误并非网络问题，而是插件更新逻辑缺陷。
- **根因**：仓库为 docs-only 仓库（`package.json` 不在远程），旧的回退逻辑在 GitHub API 限流（403）时会去读取不存在的 `package.json`，必然返回 404，导致误导性的报错。
- **修复**：
  - 移除对远程 `package.json` 的依赖；
  - 命中 GitHub API 限流（403）时明确提示「GitHub API 限流（HTTP 403）。插件与网络均正常，请稍后再试。」
  - 无正式 Release（404）时改查 releases 列表兜底，仅当确实没有发布任何版本才提示「远程仓库没有发布任何版本」。
- 新逻辑仅在异常路径触发，正常检查更新的既有行为不变。