# v1.5.71 新增：模型「功能配置」手动覆盖（视觉 / 工具调用 / 联网）

- 在「模型」页的每个模型卡片上新增 **功能配置** 下拉（视觉 / 工具调用 / 联网），默认 **自动**，检测不准时可在卡片上手动指定 **支持 / 不支持**。
- **解决痛点**：例如 Gemini 1.5 Flash 实际支持视觉，但供应商 `/models` 接口返回的 `image_input: false` 被当作自动检测结果，导致 Chat 里无法粘贴图片。现在在模型卡片上把「视觉」手动设为「支持」即可，无需改后端。
- **优先级**：手动覆盖 → 自动检测（supportsVision / supportsTools）→ 历史字段（vision / toolCalling）→ 默认值。
- 视觉 / 工具调用两项会真正注册到 VS Code 语言模型 provider 的 `imageInput` / `toolCalling` 能力位；联网仅作为本地标签展示。
- 已被模型卡片上的功能标记同步反映手动覆盖结果（VISION / TOOLS / WEB 标签随覆盖变化）。