# v1.5.73 修复：总览指标卡跟随“模型调用趋势”时间范围显示

## 问题

总览页顶部指标卡区域（`section.metric-grid`）中，「API 调用」与「Token 总量」两张卡的数据始终按固定的 30 天窗口聚合（眉标也硬编码为 `SYSTEM / 30D`），而下方的“模型调用趋势”图表右侧有独立的时间范围选择器（24 小时 / 7 天 / 30 天 / 全部 / 自定义，默认 24 小时）。切换时间范围时，指标卡数据纹丝不动，两张卡与趋势图口径不一致。

## 原因

- 主进程构建 Dashboard 状态时硬编码 `store.getUsage(30)`（30 天日聚合），指标卡从该数据取值；
- 时间范围选择是 `ModelTrendChart` 组件内部的局部 `useState`，与指标卡完全解耦，指标卡无法感知所选范围。

## 修复

- 将时间范围状态（`timeRange` / `customStart` / `customEnd`）从 `ModelTrendChart` 提升到 `Overview` 组件，图表改为受控组件，选择器行为不变（默认仍为 24 小时）；
- 「API 调用」与「Token 总量」改为基于小时级用量记录（`allUsageRecords`）按所选范围实时聚合，与趋势图的“成功调用”口径保持一致；
- 「已启用 Plan」「可用模型」保持为实时数量，不随时间变化（`LIVE DATA` 标注）；
- 眉标 `SYSTEM / 30D` 改为跟随所选范围动态显示（`SYSTEM / 24H | 7D | 30D | ALL | CUSTOM`），「API 调用」「Token 总量」卡片的角标同步显示当前范围。

## 数据口径说明

- 小时级记录中的 `requests` 为成功调用数（原始调用 − 失败），与趋势图一致；因此切到“24 小时”时指标卡数值可能与旧 30 天口径不同，属预期行为。
- 「Token 用量分布（INPUT/OUTPUT）」与失败次数快览仍基于 30 天日聚合数据，本次不在最小改动范围内。

## 升级说明

- 覆盖安装即可，无需迁移配置；数据存储结构无变化。
- 兼容 VS Code 1.104 及以上版本。
