# v1.5.76 新增：模型「上下文长度」手动覆盖

- 在「模型」页的每个模型卡片上新增 **上下文长度** 输入框，默认 **自动检测**，检测不准时可在卡片上手动输入一个上下文长度（token 数）覆盖自动值。
- **解决痛点**：某些第三方模型端点返回的上下文长度（context length / maxInputTokens）与模型实际能力不符，或自动检测值偏低，导致 Chat 可用输入 token 数偏小、长对话被过早截断。现在在模型卡片上直接填一个更大的数字即可，无需改后端。
- **优先级**：手动覆盖（`contextLengthOverride`）→ 自动检测（`maxInputTokens`）。输入框留空或点「自动」恢复为自动检测。
- 覆盖后会同步注册到 VS Code 语言模型 provider 的 `maxInputTokens` 能力位，聊天输入的 token 上限随之生效。
- 模型卡片上会实时显示当前生效的上下文长度（绿底提示手动覆盖，灰底为自动检测值）。
