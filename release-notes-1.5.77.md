# v1.5.77 新增：上下文长度 K 单位显示 + Plan 模型逐行编辑

- **「模型」页的上下文长度输入改为 K 单位**：模型卡片上的「上下文长度」输入框、占位符、提交值全部按 K（1K = 1000 Token）显示和填写，输入框右侧新增 `K` 单位角标。如想指定 128K 上下文，直接填 `128` 即可，不用再填 `128000`。
- **Plan 编辑器内每个模型可单独改上下文**：接入 / 编辑 Plan 时，每个模型行（勾选框右侧）新增一个独立的 K 单位输入框，可逐行覆盖该模型的上下文长度；留空则回退自动检测值。测试连接、切换预设、连接配置变化时草稿自动清空，避免误提交。
- **手动添加模型的上下文也改为 K 输入**：「手动添加模型」里的上下文输入框以 K 为单位（占位符 `128`），字段标签追加 `K` 角标；提交时自动换算为原始 Token 数。
- **存储与后端不变**：所有输入均在界面层做 K ↔ Token 换算（提交时 `×1000` 取整、回显时 `÷1000` 取整），底层存储与 provider 注册仍为原始 Token 数，不涉及任何数据结构或 API 变更。
- **中英文案同步**：输入提示改为「手动指定上下文长度上限（单位 K，如 128 表示 128K Token），留空则使用自动检测值」/英文 "Manually set the maximum context length in K units…"，「自动」恢复按钮保留。
